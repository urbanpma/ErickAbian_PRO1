<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EncuestasUTP" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/VerdeOscuro</item>
        <item name="colorPrimaryVariant">@color/VerdeOscuro</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/VerdeClaro</item>
        <item name="colorSecondaryVariant">@color/VerdeClaro</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/VerdeOscuro</item>
        <item name="colorPrimaryDark">@color/VerdeOscuro</item>
        <item name="colorAccent">@color/VerdeClaro</item>
        <item name="android:windowBackground">@drawable/splash_activity_backgroud</item>
    </style>
</resources>